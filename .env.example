# 数据库连接
DATABASE_URL=mysql+pymysql://root:yolo26seg@localhost:3306/yolo_seg?charset=utf8mb4

# Redis（Celery 消息队列）
REDIS_URL=redis://localhost:6379/0

# 文件存储根目录（绝对路径或相对路径）
STORAGE_ROOT=./storage

# 服务配置
HOST=0.0.0.0
PORT=8000
DEBUG=True

# 前端地址（CORS 白名单）
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]
